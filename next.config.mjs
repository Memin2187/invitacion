/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com'], // Agrega aquí el dominio de tu imagen externa
      },
};

export default nextConfig;
